import React, { useState } from 'react';


function Confirm({ route, navigation }) {
    const {
        selectedVehicle,
        markedSeats,
        registrationNumber,
    } = route.params;

    return (

     )
}

export default Confirm;